# Pacchetti opzionali del sistema di progetto

> Registro dei pacchetti opzionali che l'agente può proporre quando inizializza o allinea un progetto. Non è un elenco di cose da installare sempre: ogni voce si offre con un gate esplicito, in base al tipo di progetto, e si istanzia solo su conferma dell'utente. La skill `init-project-system` e i due prompt consultano questo file per sapere cosa proporre.

## Come si usa

Al Passo 4 dell'inizializzazione e durante l'allineamento, l'agente legge questo registro, valuta quali pacchetti sono pertinenti al progetto secondo la colonna "quando offrirlo", e li propone uno per uno con una domanda esplicita, offrendo di istanziarli ora o di rimandarli come promemoria. Non assume mai: anche un pacchetto pertinente si attiva solo se l'utente accetta. Un pacchetto già presente nel progetto non si reinstalla: se ne mostra la differenza e si chiede come procedere. Allo stesso modo, se il progetto fornisce già quella capacità in proprio (per esempio implementa già una propria knowledge base o un proprio sistema di grafo), il pacchetto non si propone come duplicato: al massimo si propone di allineare l'implementazione esistente allo standard. Quando un pacchetto viene attivato, l'agente mostra subito un recap d'uso, cioè i comandi e il flusso essenziali presi dal README del pacchetto, così l'utente sa come usarlo da subito.

Per i pacchetti di tipo MCP server vale una regola aggiuntiva: si propone un massimo di tre o quattro server nuovi per sessione, verificando che il totale dei server connessi non superi sei. Ogni server espone tool che occupano token nel contesto a ogni turno, anche se non vengono usati. Si preferisce sempre l'implementazione ufficiale del vendor (GitHub per GitHub, Sentry per Sentry) rispetto a fork community non verificati: un audit pubblico ha trovato problemi di sicurezza nel 66% dei server MCP scansionati. I server che recuperano contenuto web o documenti esterni sono vettori di prompt injection e vanno esaminati prima di concedere permessi di scrittura.

## Catalogo

| Pacchetto | Cosa fa | Quando offrirlo | Cosa istanzia | Note |
|---|---|---|---|---|
| `latex` | Ambiente di build LaTeX: manifesto pacchetti, script setup/build, skill `latex-build` | Il progetto contiene o produrra file `.tex` | `templates/latex/` in `scripts/`, `tex-packages.txt`, `.latexmkrc`, `.claude/skills/latex-build/` | Script `.ps1` e `.sh`; la distribuzione TeX resta esterna e non versionata |
| `diagrams` | Resa dei diagrammi Mermaid `.mmd` in `.svg` riusando il browser di sistema | Il progetto ha o avra diagrammi sotto `.claude/context/diagrams/` | `templates/tools/render-diagrams.mjs` in `tools/render-diagrams.mjs` | Richiede Node e un browser Chromium-based (Edge o Chrome) |
| `code-context` (MCP) | Server MCP tree-sitter che da struttura cartelle e simboli del codice | In allineamento di un progetto esistente, per far mappare all'agente la struttura del codice e popolare le schede | `.mcp.json` in radice, dalla variante OS (`templates/mcp.json` o `templates/mcp.windows.json`) | Avviato via `npx`, zero dipendenze native; nessuna cartella `mcp/` |
| `claude-code-handoff` | Riferimento distillato e versionato delle opzioni di Claude Code (flag CLI, comandi di sessione, permessi, MCP, skills, subagent, hooks) con auto-aggiornamento dalla guida community: script dual-OS che confronta la versione upstream, scarica la fonte e ri-distilla via `claude -p`, comando `/refresh-handoff`, workflow GitHub Actions opzionale | Qualsiasi progetto in cui si vuole che agente e utente abbiano sotto mano le capacità dello strumento senza riscoprirle a ogni sessione; si propone all'inizializzazione e quando l'utente chiede "quali opzioni offre Claude Code" | `templates/claude-code-handoff/` in `.claude/context/claude-code-handoff.md`, `.claude/commands/refresh-handoff.md`, `tools/update-handoff.ps1`/`.sh` (variante OS), opzionale `.github/workflows/update-handoff.yml`; stato generato in `.claude/handoff-state.json` (tracciato), fonte grezza in `_notes/upstream/` (ignorata) | Fonte Cranot/claude-code-guide, auto-aggiornata ~ogni 2 giorni quando l'updater upstream gira; gli script rilevano lo stallo della fonte (last_check upstream >14 giorni, già osservato) e avvisano; il confronto di versione è a costo zero, la ri-distillazione è una corsa LLM; in caso di divergenza dalle regole del sistema vincono le regole |
| `stack-profiles` | Profili di regole pronti per gli stack ricorrenti (ts-mcp, python, react, n8n, generico): convenzioni di stile, comandi tipici e controlli pre-commit, come regola modulare normativa | L'inizializzazione o l'allineamento riconosce uno degli stack coperti dai manifest del progetto (`package.json` con MCP SDK, `pyproject.toml`, dipendenze React, struttura n8n); `generico` è il fallback esplicito quando nessun profilo corrisponde | Un solo profilo da `templates/stack-profiles/profiles/<profilo>.md` in `.claude/rules/stack-profile.md` | Normativo e complementare alla scheda descrittiva `STACK.md`, che resta il posto dello stack reale; il file istanziato si personalizza col progetto; deriva dal bundle Cranot dell'utente |
| `hooks-starter` | Tre hook di automazione pronti, in doppia forma .ps1/.sh e mai attivi di default: `session-context` (SessionStart: branch, commit, file modificati e testa di memory/index.md nel contesto), `protect-sensitive-files` (PreToolUse su Write/Edit), `secret-scan` (PreToolUse su Bash per i git commit) | Il progetto vuole automatizzare la procedura di ripresa della sezione 12 o concretizzare i guard-rail della sezione 14 di PROJECT-SYSTEM.md; `session-context` è quello a più alto valore e si propone per primo | `templates/hooks-starter/hooks/` in `.claude/hooks/` (variante OS); l'attivazione resta manuale copiando i soli blocchi voluti da `settings.hooks.<os>.json` in `.claude/settings.json` | Blocco via exit code 2 con motivo su stderr; script difensivi (input non parsabile = lascia passare) e di sola lettura; `secret-scan` non scatta finché il deny di baseline su `git commit` resta in vigore, è difesa in profondità; deriva dal bundle Cranot dell'utente |
| `automation-starter` | Copre i livelli headless e routine di Claude Code restando dentro l'uso incluso nei piani Team o Max: wrapper dual-OS per invocare `claude -p` fuori da una sessione interattiva, workflow GitHub Actions opzionale autenticato via token di abbonamento, guida alla terza via nativa `/schedule` | Il progetto vuole automatizzare un task ricorrente (report di drift, controllo periodico, promemoria) senza tenere un terminale aperto, restando nella quota dell'abbonamento invece di aprire una chiave API a consumo | `templates/automation-starter/` in `tools/headless-run.ps1`/`.sh`, opzionale `.github/workflows/automation-routine.yml` + `.github/automation-routine-prompt.txt`; output di ogni corsa in `_notes/automation/` (ignorato) | Pacchetto separato e alternativo a `claude-code-handoff`, che usa invece `ANTHROPIC_API_KEY` a consumo nel proprio workflow per quella singola pipeline: qui l'autenticazione è sempre `claude login` in locale o `CLAUDE_CODE_OAUTH_TOKEN` da `claude setup-token` su GitHub Actions, mai una chiave API; lo script locale si rifiuta di girare se rileva `ANTHROPIC_API_KEY` nell'ambiente; la terza via nativa (`/schedule`) non richiede istanziazione ma i suoi limiti di quota per piano restano da verificare sull'account dell'utente; originale al template, non derivato dal bundle Cranot |
| `dev-skills` | Quattro skill di sviluppo da scegliere una per una: `test-generator` (test sui pattern e framework già presenti), `mcp-tool-scaffold` (tool MCP con contratto, validazione e description orientate all'LLM), `code-review` e `security-review` (varianti di progetto delle revisioni, versionate col repo) | Progetti con sviluppo attivo che vogliono procedure versionate e condivise da un clone; `test-generator` e `mcp-tool-scaffold` non duplicano nulla, le due di review si propongono solo dichiarando la sovrapposizione con le skill native `/code-review` e `/security-review` e con gli agenti di `subagent-template` | `templates/dev-skills/skills/<skill>/` in `.claude/skills/<skill>/`, una per una a scelta al gate | Le skill di review omonime mettono in ombra le native dentro il progetto: scelta esplicita, oppure si istanziano con nome distinto; deriva dal bundle Cranot dell'utente |
| `md-unwrap` | Formatter Markdown a diff minimo che srotola i paragrafi hard-wrapped: il testo di ogni paragrafo, voce di elenco e citazione torna su una riga sorgente unica, senza toccare nient'altro (marcatori di lista, tabelle, stili di titolo, escaping e ordine restano identici); ogni file passa per un'invariante interna e, se `markdown-it-py` è presente, per un oracolo di rendering che rifiuta la scrittura se l'HTML normalizzato cambierebbe; CLI batch con `--check`, `--diff`, esclusioni e marcatore `.md-unwrap-ignore` per proteggere sottoalberi | Qualsiasi progetto che scrive documentazione Markdown a mano, cioè di fatto tutti quelli che adottano il sistema: attua meccanicamente la convenzione della regola `interaction-style.md` ed è quello che rende leggibili i diff dei documenti. Si propone all'inizializzazione e, in allineamento, quando i `.md` del progetto risultano hard-wrapped a colonna fissa | `templates/md-unwrap/tools/md-unwrap.py` in `tools/md-unwrap.py`; la suite `templates/md-unwrap/tests/` si porta nel progetto solo se in quel progetto si intende modificare lo strumento | Python 3, zero dipendenze obbligatorie; `markdown-it-py` è una dipendenza di sviluppo che abilita l'oracolo di rendering (`--oracle require` la pretende); originale al template; validato sui 104 file Markdown di questo repository (50 da modificare, 1316 righe unite, nessun file saltato) e coperto da 91 controlli automatici; alternativa esterna `prettier --prose-wrap never`, più maturo ma non a diff minimo perché normalizza anche liste, tabelle, titoli ed escaping: confronto completo nel README del pacchetto |
| `fix-typography` | Tre strumenti che attuano le convenzioni tipografiche della regola `interaction-style.md`: `fix-accents.py` converte gli accenti scritti con l'apostrofo negli accenti veri distinguendo acuto e grave secondo grammatica, con regola per suffisso sulle uscite in a, i, o, u e lista per quelle in e, `fix-missing-accents.py` ripristina gli accenti mancanti del tutto nelle sole forme che senza accento non sono parole italiane, riportando come indecidibili le altre, e `fix-dashes.py` normalizza cinque segni distinti che somigliano a un trattino, compreso il segno meno matematico. Entrambi hanno autotest interno, saltano i blocchi di codice e i code span nei Markdown, e nei file di codice usano un riconoscitore più stretto che non confonde l'apostrofo di chiusura di una stringa con un accento | Qualsiasi progetto che scrive prosa italiana, cioè di fatto tutti quelli che adottano il sistema. Si propone all'inizializzazione insieme a `md-unwrap`, di cui e il complemento: quello attua la convenzione della riga sorgente unica, questo quella tipografica | `templates/fix-typography/tools/` in `tools/`, tutti e quattro i file compreso `dashes-exclude.txt` | Python 3, zero dipendenze; originale al template; nato dalla constatazione che nel template stesso convivevano tre grafie della stessa parola, e validato su tre repository con oltre ottomila sostituzioni complessive. Le esclusioni dei trattini vanno dichiarate con il motivo, e il caso da conoscere e lo strumento `docx-to-docs`, la cui tabella di sostituzione contiene quei caratteri per poterli rimuovere |
| `anonymization` | Guard-rail che cerca nei file del repository i **dati che identificano** persone, luoghi e infrastrutture reali, categoria distinta e complementare rispetto ai segreti: indirizzi IP, indirizzi fisici di apparati, nomi propri, caselle di posta personali, numeri di telefono, importi, IBAN, partite IVA. Lo strumento e' pubblicabile e non contiene alcun valore reale, perche' cio' che deve cercare vive in un file di pattern nel layer privato del progetto: un indirizzo ha una forma riconoscibile, un nome proprio no, ed e' la ragione per cui l'elenco non puo' vivere nel codice. Il perimetro predefinito e' cio' che sta per essere pubblicato, cioe' i file tracciati **piu' quelli non tracciati e non ignorati**, entrambi bloccanti; con `--tutti` entrano anche i file ignorati, elencati a parte e non bloccanti perche' contengono valori reali per costruzione. Se il file di pattern manca o non ha le chiavi obbligatorie lo strumento esce con codice 2 invece di dichiararsi verde | Il repository e' pubblico, o condiviso piu' largamente di chi lo scrive, **e** la materia documentata appartiene a qualcun altro: infrastruttura aziendale o di un cliente, apparati, persone. Le due condizioni valgono insieme. Non si propone per un repository che documenta soltanto codice proprio, dove produrrebbe soli falsi positivi; si propone anche per un repository **privato** che documenta l'infrastruttura di un cliente, perche' la cerchia di chi vi accede cambia nel tempo mentre la storia git no | `templates/anonymization/tools/Test-Anonymization.py` in `tools/` (o `scripts/`), `templates/anonymization/rules/anonymization.md` in `.claude/rules/` fra le regole da caricare sempre, e `templates/anonymization/patterns.example.json` copiato **nel layer privato** come `_notes/.anonymization-patterns.json` con i valori veri; accanto ad esso la mappa dei segnaposto `_notes/.anonymization-map.md` e il file di sostituzioni per l'eventuale pulizia della storia, entrambi ignorati | Python 3, zero dipendenze; non apre connessioni, non usa credenziali, non scrive nulla. Originale al template, estratto da un progetto reale di documentazione di rete. **Non sostituisce `hooks-starter/secret-scan` e non ne e' sostituito**: quello cerca credenziali per forma sul solo diff in stage e ferma chi le introduce, questo cerca dati identificanti per valore sull'intero perimetro e trova cio' che c'era prima che il controllo esistesse. I numeri che giustificano il pacchetto vengono dal progetto di origine: il primo audit trovo' centoquarantotto riscontri su ottantanove file, **nessuno introdotto di proposito e nessuno appartenente alla sessione che li scopri'**, e il peggiore non era un indirizzo ma una voce di registro che pubblicava la corrispondenza fra un segnaposto e la persona reale, cioe' il dato che rende reversibile ogni altra anonimizzazione. Il perimetro dei soli file tracciati, che era la prima versione, e' stato corretto dopo aver constatato che un file **nuovo** non ancora aggiunto all'indice era invisibile proprio nel momento in cui serviva guardarlo. Due limiti dichiarati e non risolvibili con piu' codice: non distingue un falso positivo da una fuga quando il valore e' ambiguo, e non conosce il contesto, quindi la lista delle eccezioni di contesto va tenuta a mano e **corta** |
| `alignment` | Controllo eseguibile che dice quali affermazioni del progetto stanno invecchiando in silenzio: le scadenze scritte in prosa (licenze, abbonamenti, contratti) con la loro conseguenza, l'eta' di una misura contro la cadenza dichiarata per la fonte, le domande che solo una persona puo' chiudere con la loro validita', e quattro invarianti fra cio' che i documenti affermano e cio' che sta nel repository: script non citati nella scheda dello stack, decisioni richiamate e non definite, unicita' dei numeri di un registro, e il campo di firma delle schede verificato per discendenza dall'ultima modifica di **contenuto**. Complementare a `sync-context` e non alternativo: quella misura il drift di una scheda contro il codice, questo copre le affermazioni su fatti esterni che nessun `git diff` contraddice mai. E' un test deterministico con codice di uscita, quindi vive in un hook e non consuma contesto; e' rumoroso e non blocca, e distingue il giallo di cio' che invecchia dal rosso di cio' che e' gia' rotto | Il progetto dipende da fatti esterni (licenze, abbonamenti, misure da fonti vive), documenta un'infrastruttura o un sistema di terzi che cambia senza notificare, oppure e' scritto da piu' sessioni o piu' persone sugli stessi file. Non si offre a un progetto di solo codice, dove il posto giusto per un invariante e' la suite di test | `templates/alignment/tools/Test-Allineamento.py` in `tools/`, `scadenze.example.json` in `data/scadenze.json` (tracciato), `rules/affermazioni-verificabili.md` in `.claude/rules/`, piu' la riga di hook `SessionStart` in `settings.json` | Python 3.8+, git nel PATH, nessuna dipendenza esterna. I percorsi del progetto si dichiarano nel blocco `percorsi` del registro, e una voce a `null` rende l'invariante corrispondente non applicabile invece di farlo fallire. Il registro va tracciato e va tenuto pulito come un documento: date e riferimenti, mai importi ne' nomi. Senza il registro esce con codice 2 invece di dichiarare un verde non calcolato |
| `community-sources` | Tre strumenti per le fonti che vivono fuori da ciò che gli strumenti di sessione raggiungono. `fetch-discord.py` legge la cronologia di un canale, di una discussione o di un post di forum attraverso un bot account ufficiale, con impaginazione, gestione del limite di frequenza in modo reattivo e preventivo, ripresa dopo i guasti transitori, cursore per leggere il solo delta fra due corse, filtri per parola chiave, lunghezza e data, e un presidio che rifiuta un token che non sia di un bot, perché la differenza fra le vie non è visibile nell'esito di una singola richiesta ma nelle conseguenze sull'account. `export-discord.py` orchestra invece l'esportatore di terze parti DiscordChatExporter su un insieme di canali scelti, per le esportazioni ampie e rare e per i server dove un bot non può entrare: raggruppa i canali per priorità, salta ciò che è già esportato, prosegue sui fallimenti, ha una prova a vuoto, e chiede la credenziale in modo interattivo perché PowerShell registra la cronologia dei comandi in chiaro su disco. La sua tabella dei canali è la parte che vale: ogni voce dichiara a quale domanda aperta del progetto quel canale risponde, perché un canale scelto per argomento produce materiale da leggere e uno scelto per domanda produce risposte. `fetch-reddit.py` risolve invece un problema di forma diversa: la fonte non è un canale ma un post che rinvia ad altri post, quindi la conoscenza sta nel grafo e non nel nodo di partenza. Attraversa quel grafo in ampiezza, che è insieme la scelta economica, perché l'archivio accetta cinquecento identificativi per richiesta, e quella che decide bene cosa lasciare fuori quando un tetto si esaurisce, perché taglia il materiale più lontano dal punto di partenza. Produce l'elenco dei nodi in `_INDEX.md` e la mappa di chi linka che cosa in `MAPPA.md`, con il grafo in `mappa.json`: i tetti dichiarano ciò che hanno escluso e i collegamenti esterni si distinguono in scaricati, catalogati con il motivo e falliti con il codice, perché contarli insieme darebbe una falsa impressione di copertura. Non usa credenziali, perché passa da un archivio pubblico di terze parti e non dall'API ufficiale, che è risultata inaccessibile | Il progetto consulta come fonte tecnica canali di community, che per certi domini sono la sola documentazione esistente, e la copia manuale non basta più per frequenza. Non si propone se i canali che servono stanno su server di cui l'utente non è amministratore e non intende chiedere l'invito: là la via resta la consegna manuale della regola. Per il solo lato Reddit il gate è diverso e più largo, perché non dipende dal consenso di nessuno: si propone quando il progetto ha come fonte discussioni pubbliche di Reddit, tipicamente un dominio tecnico o di prodotto la cui conoscenza sta nei thread, e la ragione per non proporlo non è un permesso ma la fedeltà, cioè un progetto che deve citare lo stato corrente di un thread e non uno stato archiviato di qualche giorno prima | `templates/community-sources/tools/` in `tools/`, tre file, copiabili anche separatamente perché non dipendono l'uno dall'altro; il token del bot in `.env` scritto a mano dall'utente, quello personale mai in un file; cursori in `_notes/.discord-cursori.json` e export in `_notes/fonti/dce/` (ignorati); DiscordChatExporter fuori dal repository, con il percorso in `DCE_PATH`; per `fetch-reddit.py` nulla in `.env` e nessuna registrazione da fare, e una cartella per corsa in `_notes/fonti/reddit-<sub>-<id>-<data>/` (ignorata) con indice, mappa, stato per la ripresa e materiale grezzo accanto al derivato | Python 3, zero dipendenze; originale al template. La regola `.claude/rules/web-sources-not-fetchable.md` è il criterio e va istanziata con esso: il pacchetto è lo strumento. Un bot entra in un server solo su invito di chi ha il permesso di gestione, e non esiste altra via: il cancello non è sui dati ma sul bot, e una via lecita non è una via disponibile. Alternativa raccomandata altrove, un server MCP dedicato, corretta per un agente residente e sproporzionata qui, dove il lavoro è deterministico; le due non si escludono perché usano lo stesso bot account. Validato su un progetto reale, vedi `CASE-STUDIES.md`. Il lettore di Reddit non manca più ma non è quello che era stato dichiarato mancante: non usa credenziali applicative, perché quella via è risultata inaccessibile, e passa da un archivio pubblico di terze parti. La sostituzione va conosciuta perché le due non sono equivalenti: quella dichiarata avrebbe letto Reddit, questa legge una copia di Reddit, con latenza verso il presente e memoria di ciò che è stato cancellato, quindi ciò che se ne cita si annota come tratto da un archivio. Restano mancanti il convertitore di export di chat e il ripulitore di sottotitoli, e il secondo non è più un'assenza teorica perché ogni video incontrato in un grafo di Reddit finisce catalogato con quella ragione accanto |
| `hermes-agent` | Agente residente auto-ospitato di Nous Research, uscito nel febbraio 2026: gira come servizio su una macchina propria e porta tre cose che una sessione interattiva non ha. Un gateway di messaggistica unico che si collega a molte piattaforme insieme, fra cui Telegram, Discord, Slack, Signal, Matrix, Teams ed email, gestendo le sessioni; uno scheduler cron interno che valuta i lavori dovuti a intervalli brevi, con consegna dell'esito su qualunque piattaforma collegata, e una modalità a solo script che esegue un lavoro periodico senza passare dal modello e quindi a costo di token nullo; e un client MCP con cui si dichiarano i server esterni da avviare come sottoprocessi, così che l'agente disponga dei loro tool durante il ragionamento. Espone anche un servizio compatibile con l'interfaccia di completamento conversazionale e un proxy locale per gli abbonamenti autenticati via OAuth | Il progetto ha bisogno di un presidio che agisca quando nessuno ha una sessione aperta, per esempio un digest periodico di una fonte, un controllo notturno o un promemoria consegnato su una chat; oppure di essere interrogabile da una chat mentre si è altrove. Non si propone quando il bisogno è episodico, cioè leggere qualcosa quando nasce una domanda: là un programma deterministico chiamato a mano costa incomparabilmente meno di un agente residente con il proprio modello, e la regola `token-economy.md` prescrive la prima via. Non si propone nemmeno come sostituto della modalità headless di `automation-starter`, che copre il caso più semplice restando dentro l'abbonamento | Nessun file nel progetto: è un servizio esterno da installare e mantenere sulla propria macchina, tipicamente come servizio di sistema, con i segreti in un file cifrato e non nel repository. Ciò che eventualmente entra nel progetto è la sola documentazione di quali lavori periodici esistono e che cosa producono | Open source e gratuito, auto-ospitato. Verificato in sessione che il progetto esiste, chi lo pubblica, la data di uscita e le tre capacità sopra; NON installato né eseguito, quindi tutto ciò che riguarda l'esperienza d'uso resta da validare. Due avvertenze dalla ricerca. La prima è che il suo adattatore Discord nativo è reattivo, cioè risponde quando qualcuno gli scrive, e non espone all'agente un tool per leggere a comando la cronologia arbitraria di un canale: per quello serve un server MCP dedicato, vedi la riga seguente, oppure il programma del pacchetto `community-sources`. La seconda è che l'ampliamento della cerchia di chi può scrivere all'agente amplia anche ciò che quella cerchia gli può far fare, quindi la lista di autorizzazione va pensata insieme ai tool concessi e non dopo. Il costo in token di un lavoro periodico che rilegge ogni volta la stessa fonte cresce linearmente e senza motivo: va tenuto un cursore di ciò che è già stato processato, e va riservata al modello la sola sintesi finale |
| `discord-mcp` (MCP) | Server MCP della community che espone a un agente i tool per leggere e cercare in canali Discord attraverso un bot account ufficiale, cioè la stessa via legittima del pacchetto `community-sources` ma disponibile come tool durante una conversazione invece che come programma da riga di comando | Progetti con un agente residente, per esempio `hermes-agent`, in cui l'agente deve poter decidere lui quando leggere un canale nel mezzo di un ragionamento. Non si propone quando il compito è deterministico, cioè leggere una fonte e trasferirne la sintesi in un registro: là vale `community-sources`, che non aggiunge un ambiente di esecuzione, un pacchetto di terze parti a cui affidare una credenziale e uno strato di protocollo interposto | Configurazione del server nel client MCP che lo ospita, con il bot token passato dall'ambiente e mai in chiaro in un file tracciato | Tutti community, nessuno ufficiale né di Discord né del progetto che li ospita: da scegliere leggendo il codice, perché ricevono un token. Esistono due famiglie e una va evitata: quella che si autentica con un bot token del portale per sviluppatori, che è l'unica ammissibile, e quella che usa il token dell'account personale, spesso ottenuto dal traffico del client, che è self-botting e che le condizioni d'uso vietano con rischio di terminazione dell'account. Dalla ricerca, non verificata in sessione: fra i progetti della prima famiglia uno espone la lettura con impaginazione a cursore, un altro aggiunge la ricerca a testo pieno nel server, e un terzo permette di selezionare quali insiemi di tool esporre tramite una variabile d'ambiente, che è la proprietà da preferire perché consente di concedere i soli tool di lettura escludendo moderazione e gestione dei ruoli. Il criterio di scelta è quello della regola `web-sources-not-fetchable.md`, e il principio del minimo privilegio vale sui tool esposti come sui permessi del bot |
| `knowledge-wiki` | LLM Wiki: `sources/` immutabile, `wiki/` compilata dall'LLM, schema `WIKI-SCHEMA.md`, skill `wiki-digest` | Progetti dove si accumula conoscenza trasversale nel tempo, se non già coperta da una knowledge base nativa | `templates/knowledge-wiki/` in `knowledge/` (`WIKI-SCHEMA.md`, `sources/`, `wiki/`, `log.md`) e `.claude/skills/wiki-digest/` | Pattern Karpathy; vedi knowledge-systems-analysis |
| `book-to-skill` | PDF tecnico in skill pre-digerita on-demand, via la skill `book-digest` | Progetti con libri o PDF tecnici di riferimento | `templates/book-to-skill/` in `.claude/skills/book-digest/`; le skill-libro `<slug>/` le genera `book-digest`, locali per default, globali solo su conferma | Pattern book-to-skill; ponte opzionale verso `knowledge-wiki` (path A/B) |
| `docx-to-docs` | Converte un `.docx` voluminoso in un albero `docs/` Markdown navigabile e versionato: un file per sezione, `README.md` indice generati che linkano ai figli, hub `DEVELOPMENT.md`; conversione deterministica e idempotente con annotazioni (banner LEGACY), redazioni e pulizia `--clean` | Il progetto parte da un `.docx` o da un corpus scritto a mano da rendere documentazione tecnica navigabile; complementa l'ingestione a fette, che invece non versiona. Per più documenti insieme, non per la conversione in documentazione, vedi `doc-ingest` | `templates/docx-to-docs/` in `tools/docx-to-md.py` (+ `annotations.json`); output in `docs/`; `redactions.json` locale | Richiede `python-docx`; il `.docx` resta locale come fonte di rigenerazione; verifica di completezza e report integrati |
| `doc-ingest` | Ingestione incrementale di un corpus di documenti voluminosi (`.pdf`, `.docx`, `.pptx`, `.xlsx`, `.html`) in una cache Markdown locale a zero token: manifest a content-hash per non riconvertire l'invariato, indice `_INDEX.md` di Livello 1 (titoli, conteggi, stato) rigenerato a ogni corsa, motore veloce di default con modalità accurata (`--engine docling`) e fallback OCR (`--ocr`) opzionali | Il progetto ha o ricevera più documenti PDF/DOCX/office voluminosi da consultare durante il lavoro senza versionarli; complementa `docx-to-docs`, che invece produce documentazione versionata da un solo documento | `templates/doc-ingest/` in `tools/doc-ingest.py`; cache locale in `_notes/.tmp-doc-cache/` | Richiede `markitdown`; `docling` e `pytesseract`+`pdf2image` (+ binario `tesseract-ocr`) sono opzionali, solo se si usano i rispettivi flag; su scansioni di bassa qualità l'OCR è spesso inaffidabile, vedi `book-bib-extract` per il fallback pratico quando serve solo l'anagrafica bibliografica |
| `book-bib-extract` | Estrae anagrafica bibliografica (autore, titolo, anno, editore, edizione, ISBN) da libri già ingeriti con `doc-ingest`, propone una citekey e aggiorna un registro unificato a tre stati (verificata/da-verificare/scartata); per PDF scansionati senza testo nativo, verifica visiva del colophon (via `pdftoppm`) invece del mirror Markdown vuoto | Il progetto costruisce una bibliografia da libri fisici o scansionati senza DOI (manoscritti, libri di studio posseduti); copre un vuoto tra `doc-ingest` (estrae struttura, non anagrafica) e `academic-researcher` (pensato per paper con DOI, non libri) | `templates/book-bib-extract/` in `.claude/skills/book-bib-extract/`, `tools/extract-titlepages.py`, `tools/render-bib-registry.py`; registro in `_notes/book-bib-registry.json` (ignorato) | Condivide il registro con `book-digest` (campo `skill_status`, di proprietà esclusiva di quel pacchetto): un solo registro, non due sistemi di stato paralleli; non scrive mai una voce `.bib` senza conferma umana esplicita contro il colophon; validato su un progetto reale (153 voci di registro), vedi `CASE-STUDIES.md` |
| `notebooklm-bridge` | Metodo di ricerca fondata NotebookLM (gratuito) + Claude: loop a 4 passi (NotebookLM sintetizza con citazioni, Claude ragiona sulla sintesi, rimanda a NotebookLM cio che va verificato, Claude scrive la versione finale), preceduto da una verifica di disponibilità dell'account Google/browser; tiene il corpus di fonti fuori dal contesto di Claude e vi fa ragionare solo la sintesi citata | Il progetto ragiona o scrive a partire da un corpus di fonti proprie dell'utente e conviene tenerlo fuori dal contesto di Claude; è l'incrocio di token economy e ricerca. Non si propone se il progetto non ha fonti proprie da caricare o se basta la ricerca web (skill `deep-research`) | `templates/notebooklm-bridge/` in `.claude/skills/notebooklm-bridge/` e `tools/notebooklm-check.ps1`/`.sh` (variante OS); cache locale in `_notes/notebooklm/` | Piano gratuito tassativo (solo browser, nessuna API Enterprise a pagamento); modo manuale conforme di default, modo assistito via `notebooklm-mcp` opt-in; deriva da screenshot di un metodo pubblico condiviso dall'utente, non ancora validato su un progetto reale |
| `notebooklm-mcp` (MCP) | Server MCP della community che pilota il NotebookLM gratuito nel browser via automazione, riusando un profilo Chrome con login Google persistito e leggendo le citazioni dal DOM; abilita il modo assistito del pacchetto `notebooklm-bridge` | Progetti con `notebooklm-bridge` attivo che ripetono il loop spesso e vogliono che Claude piloti NotebookLM invece del copia-incolla manuale; opt-in a ragion veduta | `.mcp.json` in radice con la configurazione del server scelto | Community, NON ufficiale; l'automazione del browser sul piano gratuito vive in zona grigia dei ToS (l'unico endpoint conforme è l'API Enterprise a pagamento, fuori perimetro). Raccomandato `PleasePrompto/notebooklm-mcp` (MIT, Patchright); alternative `jacob-bd/notebooklm-mcp-cli`, `julianoczkowski/notebooklm-mcp-2026`; in alternativa `playwright-mcp` come automazione generica del browser |
| `academic-researcher` | Ambiente di ricerca accademica: scoping del topic, tracciamento tripartito delle fonti (verificata/da verificare/scartata), sincronizzazione Zotero-JabRef del `.bib`, e la Corpus Analysis Suite (10 prompt collaudati) per analizzare un corpus di paper già caricato | Il progetto è di ricerca accademica, di tesi, o richiede analisi sistematica di un corpus di paper; non si propone se il progetto ha già un proprio sistema bibliografico equivalente | `templates/academic-researcher/` in `.claude/skills/` (8 skill), `.claude/rules/no-uncited-claims.md`, `research-vault/` (documento di riferimento, `.bib`, scope) | 5 skill complete (corpus-analysis, citation-tracker, bib-sync, research-scoping, literature-search), 3 parzialmente stub da calibrare all'attivazione (deep-paper-reading, gap-analysis, skill-autogen); si abbina alle righe MCP e tool esterni sotto |
| `learning-agent` | Trasforma il progetto in un tutor di apprendimento guidato: profilo learner con onboarding a bottoni, roadmap con template pedagogico per categoria (lingua, programmazione, materia concettuale), tre subagent (`tutor`, `kb-retriever`, `examiner`) e tre skill/comandi (`/profile`, `/learn`, `/review`) | Il progetto ha come scopo dichiarato l'apprendimento personale di un topic specifico a partire da una knowledge base di file dell'utente, non lo sviluppo di un prodotto; non si propone se il progetto ha già un proprio tutor o spaced repetition equivalente | `templates/learning-agent/` in `.claude/agents/` (3 file), `.claude/skills/` (3 skill), `.claude/commands/` (3 comandi), `LEARNER_PROFILE.md` in radice, `.claude/context/learning-agent-reference.md` | Deriva da ricerca dell'utente di luglio 2026, non da un modulo già rodato: il livello di orchestrazione è completo da subito, la scelta dei MCP di knowledge base e Anki resta al gate (righe `knowledge-mcp` e `anki-mcp` sotto) |
| `knowledge-mcp` (MCP) | Knowledge base locale con RAG ibrido vettoriale più grafo, basato su LightRAG: ingesta PDF/DOCX/Markdown, estrae entità e relazioni, espone la ricerca via FastMCP | Progetti con `learning-agent` attivo che vogliono una knowledge base interrogabile sui file dell'utente (libri, dispense); adatto a topic strutturati con dipendenze concettuali | `.mcp.json` in radice con configurazione knowledge-mcp | Community; richiede Python 3.12 e `uv`; repo olafgeibig/knowledge-mcp; alternative: Local Knowledge RAG MCP di patakuti (100% locale via Ollama), mcp-local-rag di nkapila6 (fallback di ricerca web locale) |
| `anki-mcp` (MCP) | Spaced repetition via proxy locale verso AnkiConnect: creazione/modifica note, gestione deck, sessioni di review conversazionali, nessuna telemetria | Progetti con `learning-agent` attivo che vogliono automatizzare lo scheduling del ripasso invece di tracciarlo solo in `LEARNER_PROFILE.md` | `.mcp.json` in radice con configurazione anki-mcp-server | Community; richiede Anki desktop e l'add-on AnkiConnect (plugin id 2055492159); repo ankimcp/anki-mcp-server (ankimcp.ai); alternativa minimale: mcp-ankiconnect di samefarrar (solo il loop di ripasso) |
| `codebase-learning` | Trasforma un progetto finito o quasi finito di sviluppare in un ambiente di apprendimento guidato della repository stessa: comando `/learn-repo` a cinque fasi (panoramica, astrazioni centrali, flussi, design puro, idiomi dello stack) e subagent `code-tutor` che cita sempre path e riga reali, appoggiandosi ai grafi strutturali già in catalogo (`serena`, `repomix`, `code-context`, `codebase-memory-mcp`, `graphify`) invece di grep a tappeto | Il progetto è dichiarato finito o quasi finito di sviluppare e l'utente vuole essere guidato nel capirne l'architettura, i paradigmi e i flussi invece di continuare a modificarlo; non si propone se il progetto è ancora in sviluppo attivo, o se serve solo un onboarding rapido già coperto dalla skill `onboard` | `templates/codebase-learning/` in `.claude/agents/code-tutor.md`, `.claude/skills/learn-repo/`, `.claude/commands/learn-repo.md`, `.claude/context/codebase-learning-reference.md` | Deriva per intero da un handoff di ricerca dell'utente di luglio 2026, non ancora validato su un progetto reale; distinto da `learning-agent`, che insegna un topic esterno da una knowledge base personale, non la repository corrente |
| `serena` (MCP) | Grafo semantico del codice via language server (LSP): navigazione a livello di simbolo (`find_symbol`, `find_references`), non di testo, su 20-40+ linguaggi, riusando gli stessi language server di VS Code; cache semantica dedicata | Progetti con `codebase-learning` o `code-context` attivo dove la ricerca testuale non basta più (repo grandi o multi-linguaggio) | `.mcp.json` in radice con configurazione serena, dopo `uv tool install -p 3.13 serena-agent` e `serena init` | MIT; repo oraios/serena; alternativa più pesante di `code-context` (tree-sitter), da scegliere quando serve risoluzione semantica cross-file |
| `repomix` (MCP) | Impacchetta l'intera codebase, locale o remota, in un singolo file consolidato ottimizzato per l'AI: metriche, file tree, contenuto formattato | Progetti con `codebase-learning` attivo, come vista consolidata iniziale prima di scendere nel dettaglio con `serena` o il subagent `code-tutor` | `.mcp.json` in radice con configurazione `npx -y repomix --mcp` | MIT; repo yamadashy/repomix (repomix.com) |
| `deepwiki-skill` | Auto-genera documentazione di apprendimento con citazioni precise a riga: pipeline analyze -> generate (subagent in parallelo) -> validate (accuratezza dei riferimenti) | Progetti con `codebase-learning` attivo che vogliono persistere il materiale di apprendimento in `docs/learning/` invece di consumarlo solo in sessione | Skill esterna Claude Code: si clona in `.claude/skills/deepwiki/` (locale al progetto, preferito; globale solo su conferma) | OSS; richiede Python 3.12+; repo natsu1211/deepwiki-skill |
| `deepwiki-mcp` (MCP) | Q&A architetturale in linguaggio naturale su repository GitHub pubbliche già indicizzate (codice, README, issue) | Progetti con `codebase-learning` attivo il cui repository è effettivamente pubblico; mai per codice proprietario, l'indicizzazione pubblica non copre repo privati | `.mcp.json` in radice con `claude mcp add -s user -t http deepwiki https://mcp.deepwiki.com/mcp` | Gratis solo per repo pubblici; endpoint ufficiale mcp.deepwiki.com |
| `caveman` | Riduce ~65% dei token di output facendo rispondere l'agente in modo telegrafico, senza toccare il ragionamento | Sessioni operative o coding output-heavy; NON quando il progetto produce documentazione o prosa | Tool esterno di sessione, installato su conferma (skill/plugin Claude Code); nessun file nel progetto | Vedi `rules/token-economy.md`; repo juliusbrussee/caveman |
| `graphify` | Grafo di conoscenza dell'intero progetto, codice e documenti: relazioni, community, nodi centrali e report di insight, con visualizzazione interattiva | Capire in modo olistico un progetto grande o sconosciuto, o una collezione di documenti; complementare a `code-context` (simboli) e a `knowledge-wiki` (pagine curate) | Tool esterno: `uv tool install graphifyy` o `pipx install graphifyy`, poi `graphify install`; si usa con `/graphify .` | MIT; codice via tree-sitter locale, documenti via LLM (consuma token); repo safishamsi/graphify |
| `arxiv-cli` | CLI standalone (non MCP) per recupero rapido dei paper arXiv più recenti per categoria/query, senza demone da mantenere | Progetti con `academic-researcher` attivo, come primo passo veloce di `literature-search` o per monitoraggio periodico di un topic | Binario nel PATH, installato via `npm`, `go install` o `cargo install`; invocato da bash, nessun file nel progetto | MIT; repo AstraBert/arxiv-cli; il testo dei paper scaricati resta input non fidato (vedi regola `no-uncited-claims` del pacchetto) |
| `grobid` | Motore di riferimento per trasformare un PDF scientifico in XML/TEI strutturato: titolo, abstract, autori, keyword e parsing dei riferimenti bibliografici (F1-score >0.87) | Progetti con `academic-researcher` attivo che leggono paper in profondità (skill `deep-paper-reading`); richiede Docker | Container Docker (`docker run --rm -it --init -p 8070:8070 lfoppiano/grobid:0.8.0`); nessun file nel progetto | Open source dal 2011, mantenuto da Luca Foppiano/Inria; usato anche da CORE e da pipeline RAG scientifiche come PaperQA2 |
| `paperqa2` | Framework RAG agentico per letteratura scientifica: risposte con citazioni in-testo verificate, re-ranking contestuale, controllo retrazioni, compatibile con Claude via LiteLLM | Progetti con `academic-researcher` attivo che vogliono risposte su un corpus di PDF con citazioni verificate invece di lettura manuale | Tool esterno installato via pip; nessun file nel progetto | Open source; licenza da verificare all'installazione; repo Future-House/paper-qa; riferimento Skarlinski et al. 2024 |
| `humanizer` | Skill che rimuove i segni di scrittura AI dal testo: frasi riempitive, regola del tre, trattini lunghi e grassetto in eccesso, hedging, tono promozionale (33 pattern, con audit finale) | Progetti che producono prosa o testo (blog, libro, documentazione) | Skill esterna: si clona in `.claude/skills/humanizer/` (locale al progetto, preferito; globale solo su conferma); si usa con `/humanizer <testo>` | MIT; complementa la regola `interaction-style`; repo blader/humanizer |
| `taste-skill` | Skill di design-taste: migliora layout, tipografia, spaziatura e motion delle interfacce generate, per evitare UI dall'aspetto generico-AI | Progetti con frontend o UI | Skill esterna: `npx skills add https://github.com/Leonxlnx/taste-skill` | MIT; repo Leonxlnx/taste-skill |
| `ccusage` | Misura baseline del consumo token per sessione, modello e progetto; legge i JSONL locali di Claude Code senza inviare nulla altrove | Sempre, prima di installare qualsiasi altro strumento di ottimizzazione - serve la baseline per misurare i guadagni reali | Nessun file nel progetto; `npx ccusage@latest` per report; `/usage` e `/context` per la sessione corrente | MIT; repo ryoppippi/ccusage |
| `rtk` | Comprime l'output di git, cargo, pytest, npm e oltre 100 comandi shell prima che entri nel contesto; 60-90% di risparmio sui comandi rumorosi | Progetti con test runner o CI output-pesanti su Linux o WSL2 (su Windows nativo l'auto-riscrittura richiede WSL) | Tool esterno: installer curl; hook `PreToolUse` in `settings.json` | MIT; repo rtk-ai/rtk |
| `ponytail` | Impone la disciplina "senior developer pigro" al codice generato: YAGNI, stdlib prima, minimo che funziona; 46-94% meno righe sui benchmark Haiku 4.5 | Sessioni coding output-intenso su modelli Haiku/Sonnet/Opus; non usare su modelli locali piccoli | Plugin Claude Code: `/plugin marketplace add DietrichGebert/ponytail` poi `/plugin install ponytail@ponytail` | MIT; repo DietrichGebert/ponytail |
| `codebase-memory-mcp` | Grafo strutturale del codice (call chain, rotte HTTP, dipendenze tra moduli, 158 linguaggi via tree-sitter); 10x-120x meno token su query strutturali rispetto all'esplorazione file-by-file | Progetti oltre 50K righe dove la navigazione strutturale è frequente; complementare a `code-context` (simboli) e `graphify` (grafo concettuale) | MCP server: binario statico locale, zero dipendenze; `claude mcp add codebase-memory -- <percorso-binario>` | MIT; repo DeusData/codebase-memory-mcp |
| `context-mode` | Sandboxa l'output di tool pesanti (Playwright, log server, dump issue) in SQLite locale; Claude vede solo un riferimento invece del dump grezzo | Sessioni con output di tool che superano regolarmente le decine di migliaia di token | Tool esterno di sessione; nessun file nel progetto | MIT; repo mksglu/context-mode |
| `headroom` | Comprime tutto cio che arriva all'LLM (output tool, log, file, storia conversazione) tramite proxy locale; i dati non lasciano la macchina; 47-95% secondo workload | Sessioni operative molto lunghe e eterogenee dove RTK e igiene di sessione non bastano | Tool esterno: `pip install headroom-ai[all]`; avviato con `headroom wrap claude` | MIT; repo chopratejas/headroom |
| `claude-code-router` | Instrada ogni richiesta verso il modello più economico per quel tipo di task (Haiku per task banali, Sonnet per lavoro normale, Opus per casi complessi); 3-5x risparmio sul traffico spostato | Il vincolo principale è la bolletta API; verificare la policy di compliance prima di abilitare provider non-Anthropic su dati soggetti a NDA | Tool esterno: `npm install -g @musistudio/claude-code-router`; `ccr start` per il proxy locale | MIT; repo musistudio/claude-code-router |
| `claude-mem` | Memoria semantica persistente cross-sessione (SQLite + Chroma, tutto locale); tre livelli di retrieval (indice, timeline, dettaglio) per non far esplodere il contesto al riavvio | Progetti multi-settimana con storia architetturale e debug history che il deterministico `.claude/memory/` non scala a gestire; i due approcci sono complementari, non alternativi | Tool esterno: `npx claude-mem install`; web viewer locale su `localhost:37777`; tag `<private>` per escludere contenuto sensibile | MIT; repo thedotmack/claude-mem |
| `andrej-karpathy-skills` | Skill con quattro principi comportamentali anti-over-engineering: no assunzioni silenziose, no 500 righe dove ne bastano 50, no modifiche ortogonali, no scope creep | Qualsiasi progetto con output di codice intenso; zero dipendenze runtime; complementare a `rules/interaction-style.md` | Skill esterna: clonare in `.claude/skills/andrej-karpathy-skills/` | MIT; repo forrestchang/andrej-karpathy-skills |
| `code-simplifier` | Passa il codice già scritto attraverso un filtro di semplificazione con vincolo esplicito contro il cambio di comportamento; sicuro da eseguire prima del merge | Progetti dove l'over-engineering è un rischio ricorrente; da usare selettivamente su output genuinamente complesso, non come passo di routine | Plugin ufficiale Anthropic: `/plugin marketplace add anthropics/claude-plugins-official` poi `/plugin install code-simplifier` | Ufficiale Anthropic; marketplace anthropics/claude-plugins-official |
| `github-mcp` (MCP) | Issue, PR, code search, gestione repository GitHub direttamente da Claude; il singolo MCP a maggiore impatto per chi usa GitHub | Qualsiasi progetto su GitHub con workflow di PR e issue attivo | `.mcp.json` in radice con configurazione del server GitHub ufficiale | MIT; implementazione ufficiale GitHub; repo github/github-mcp-server |
| `context7` (MCP) | Documentazione aggiornata di librerie e framework con query in un singolo trigger; risolve le allucinazioni da documentazione obsoleta su framework con release frequenti | Progetti che usano framework in evoluzione rapida (Next.js, React, FastAPI, ecc.) | `.mcp.json` in radice con configurazione Context7 | MIT; repo upstash/context7 |
| `playwright-mcp` (MCP) | Automazione browser via accessibility snapshot (non screenshot), test E2E e verifica visuale controllata; gira interamente in locale | Progetti con test E2E o con verifica visuale dell'interfaccia come parte del workflow di sviluppo | `.mcp.json` in radice con configurazione Playwright MCP | MIT; implementazione ufficiale Microsoft; repo microsoft/playwright-mcp |
| `sentry-mcp` (MCP) | Stack trace, breadcrumb e correlazione errori-release direttamente in Claude; elimina il loop copia-incolla del contesto di errore | Progetti con error monitoring attivo su Sentry | `.mcp.json` in radice con configurazione Sentry MCP (HTTP + OAuth) | Implementazione ufficiale Sentry |
| `figma-mcp` (MCP) | Accesso all'albero dei nodi Figma per implementare design pixel-accurate senza copia-incolla manuale | Progetti frontend con design file Figma come source of truth | `.mcp.json` in radice con configurazione Figma MCP (HTTP + OAuth) | Implementazione ufficiale Figma |
| `vercel-mcp` (MCP) | Monitoraggio deploy, gestione variabili d'ambiente, health check produzione vs preview | Progetti deployed su Vercel con workflow di deploy attivo; rischio medio perché può modificare env var | `.mcp.json` in radice con configurazione Vercel MCP (HTTP + OAuth) | Implementazione ufficiale Vercel |
| `linear-mcp` (MCP) | CRUD issue, sprint e cicli Linear direttamente da Claude | Team che usa Linear come issue tracker; richiede piano Standard+ | `.mcp.json` in radice con configurazione Linear MCP (HTTP + OAuth) | Implementazione ufficiale Linear |
| `zotero-mcp` (MCP) | Libreria bibliografica di lavoro: aggiunta per DOI, ricerca semantica, export BibTeX verso `research-vault/bibliography.bib` | Progetti con il pacchetto `academic-researcher` attivo che gestiscono una libreria Zotero | `.mcp.json` in radice con configurazione del server Zotero scelto | Community, non ufficiale Zotero; raccomandato `54yyyu/zotero-mcp` (semantic search, CLI), alternativa `Xevos117/mcp-zotero` se serve iniezione citazioni in `.docx` |
| `academix` (MCP) | Ricerca letteratura aggregata su OpenAlex, DBLP, Semantic Scholar, arXiv e Crossref, con export BibTeX nativo | Progetti con `academic-researcher` attivo, come base della skill `literature-search` | `.mcp.json` in radice con configurazione Academix | Community; repo xingyulu23/Academix |
| `semantic-scholar-mcp` (MCP) | Ricerca su 225M+ paper con `export_bibtex` nativo e tracking di sessione (`list_tracked_papers`) | Progetti con `academic-researcher` attivo, in combinazione con `academix` | `.mcp.json` in radice con configurazione semantic-scholar-mcp | Community; repo akapet00/semantic-scholar-mcp |
| `refchecker-mcp` (MCP) | Verifica anti-hallucination delle citazioni: `verify_citation` controlla titolo/autori/anno/DOI/arXiv-ID contro Semantic Scholar, OpenAlex e Crossref | Progetti con `academic-researcher` attivo; usato dalla skill `citation-tracker`, con fallback manuale se assente | `.mcp.json` in radice con configurazione refchecker-mcp | Community; repo JonasBaath/mcp-refchecker, costruito su academic-refchecker (MIT) |
| `subagent-template` | Quattro agenti di esempio pronti all'uso (`code-reviewer`, `security-auditor`, `debugger`, `explorer`) con frontmatter YAML e system prompt strutturato; base per definire ruoli agente specializzati del progetto | Progetti che richiedono review automatizzata, audit di sicurezza ricorrenti o investigazione sistematica dei bug, o dove si vogliono definire subagent riusabili; si scelgono uno per uno | `templates/agents/<agente>.md` in `.claude/agents/` del progetto | Documentazione ufficiale Claude Code, formato nativo; `debugger` ed `explorer` derivano dal bundle Cranot dell'utente, ed `explorer` duplica in parte l'agente nativo Explore (la variante di progetto è versionata e conosce il sistema di memoria): la sovrapposizione si dichiara al gate |
| `agent-catalog` | Pesca a richiesta, uno alla volta, subagent specializzati per stack/framework dalla fonte community `0xfurai/claude-code-subagents` (138 agenti, file Markdown flat senza plugin), con tre comandi (`/list-community-agents`, `/fetch-community-agent`, `/check-agent-sources`) e script dual-OS che confrontano il commit della fonte con quello registrato, sulla stessa strategia a costo zero di `claude-code-handoff` | Il progetto vuole un subagent iper-specifico per uno stack o un tool preciso senza scriverlo da zero, e la fonte scelta non offre un plugin nativo installabile; si propone quando emerge un bisogno puntuale (es. "serve un agente esperto di Prisma") | `templates/agent-catalog/` in `.claude/agent-catalog/sources.json` (tracciato), `.claude/commands/` (3 comandi), `tools/{list,fetch,check}-community-agent{,-sources}.ps1`/`.sh`; stato generato in `.claude/agent-catalog/state.json` (tracciato); ogni fetch scrive in `.claude/agents/<agente>.md` | Fonte `0xfurai/claude-code-subagents` (MIT) integrata come fetchable; l'indice curato `hesreallyhim/awesome-claude-code` è tracciato come fonte "meta" solo per il controllo di drift, non elencabile; per `wshobson/agents` e `VoltAgent/awesome-claude-code-subagents`, che offrono già un plugin marketplace nativo, si usano le righe di catalogo separate sotto invece del fetch di questo pacchetto; meccanismo verificato dal vivo due volte (in isolamento e dentro un progetto ospite reale nel pilota del 2026-07-02), entrambe pulite |
| `wshobson-agents` | Marketplace di plugin Claude Code nativo con 194 agent, 158 skill e 106 comandi slash organizzati per dominio (linguaggi, infrastruttura, security, dati/ML) | Il progetto vuole installare unità complete e curate di agent/skill per dominio invece di pescare singoli file, e preferisce l'aggiornamento gestito dal plugin marketplace nativo | Plugin Claude Code: `/plugin marketplace add wshobson/agents` poi `/plugin install <plugin>@claude-code-workflows` (nome del plugin dal proprio catalogo) | MIT; repo wshobson/agents, 37,4k stelle, il più mantenuto tra le fonti esaminate |
| `voltagent-subagents` | Marketplace di plugin Claude Code nativo con 154+ subagent in 10 categorie (core development, language specialists, infrastructure, quality & security, data & AI, developer experience, specialized domains, business & product, meta-orchestration, research & analysis) | Il progetto vuole installare un intero dominio di subagent già curato (es. tutti gli specialist di linguaggio) invece di pescarli uno a uno | Plugin Claude Code: `/plugin marketplace add VoltAgent/awesome-claude-code-subagents` poi `/plugin install <plugin>@voltagent-subagents` (un plugin per categoria, nomi nel proprio `marketplace.json`) | MIT; repo VoltAgent/awesome-claude-code-subagents, 22,7k stelle |
| `multiclaude` | Supervisore che assegna task a subagent definiti via file Markdown; modalità singleplayer (merge automatico) o multiplayer (review umana richiesta prima del merge) | Progetti con 3 o più feature branch paralleli dove si vuole supervisione con gate di review umana | Tool esterno: `go install github.com/dlorenc/multiclaude/cmd/multiclaude@latest` | MIT; repo dlorenc/multiclaude |
| `psmux` | Tmux nativo per Windows (no WSL, no Cygwin); legge il `.tmux.conf` esistente; first-class support per le agent teams di Claude Code in pannelli separati | Windows senza WSL2 con 3 o più sessioni Claude Code in parallelo | Tool esterno per l'utente; nessun file nel progetto | MIT; repo psmux/psmux |
| `tmux-claude-session-manager` | Plugin tmux con picker centrale (fzf) che mostra lo stato live (working/waiting/idle) di tutte le sessioni Claude Code, alimentato dagli hook nativi `UserPromptSubmit` e `Stop`; no polling | Setup con tmux e 3 o più sessioni Claude Code contemporanee | Plugin tmux (TPM): configurazione in `.tmux.conf`; nessun file nel progetto | MIT; repo craftzdog/tmux-claude-session-manager |
| `claudecode.nvim` | Plugin Neovim che implementa il protocollo MCP WebSocket delle estensioni ufficiali (VS Code, JetBrains) in Lua pura; toggle floating window, auto-reload buffer quando Claude modifica file su disco | Workflow primario in Neovim | Configurazione nel `init.lua` dell'utente; nessun file nel progetto | MIT; repo coder/claudecode.nvim; alternativa con toggle più semplice: greggh/claude-code.nvim |

## Aggiungere un pacchetto

Un nuovo pacchetto si aggiunge con una riga in questo catalogo e, se è a cartella, con una sottocartella sotto `templates/` che contiene un proprio `README.md` di istanziazione, sul modello di `templates/latex/`. La colonna "quando offrirlo" deve indicare un trigger concreto, così che il gate sappia quando proporlo senza assumere. I pacchetti che non sono cartelle, come `diagrams` e `code-context`, vivono come voci del catalogo e puntano ai file già presenti sotto `templates/`.

Il percorso completo per estendere il sistema, dalla ricerca dello strumento alla voce di catalogo fino alla validazione come case-study, è descritto nella sezione "Come estendere il sistema" del `README.md` di radice.
